services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_lien.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
